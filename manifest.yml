applications:
  - name: ewp-server
    memory: 128M
    disk_quota: 256M
    instances: 5
    buildpacks:
      - binary_buildpack
    command: bash start.sh
    random-route: true
    health-check-type: http
    health-check-http-endpoint: /
    env:
      GRPC_SERVICE: api
